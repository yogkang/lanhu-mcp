# 蓝湖 MCP 服务器配置示例
# 复制此文件为 .env 并填入你的配置值
# ⚠️ 注意：不要将 .env 文件提交到 git！

# ==============================================
# 必需配置
# ==============================================

# 蓝湖 Cookie（必需）
# 获取方法：登录蓝湖网页版，打开浏览器开发者工具，从请求头中复制 Cookie
LANHU_COOKIE="your_lanhu_cookie_here"

# ==============================================
# 可选配置
# ==============================================

# 数据存储目录
# 默认值：./data
DATA_DIR="./data"

# 服务器主机地址
# 默认值：0.0.0.0（监听所有网络接口）
# 如果只需本地访问，使用 127.0.0.1
SERVER_HOST="0.0.0.0"

# 服务器端口
# 默认值：8000
SERVER_PORT=8000

# ==============================================
# 飞书机器人配置（可选）
# ==============================================

# 飞书 Webhook URL
# 获取方法：在飞书管理后台创建机器人后获取
# 留空或不设置则禁用飞书通知功能
# 示例：FEISHU_WEBHOOK_URL="https://open.feishu.cn/open-apis/bot/v2/hook/your-webhook-key-here"
FEISHU_WEBHOOK_URL=""

# ==============================================
# 高级配置
# ==============================================

# 日志级别
# 可选值：DEBUG, INFO, WARNING, ERROR, CRITICAL
# 默认值：INFO
LOG_LEVEL="INFO"

# 日志目录
# 默认值：./logs
LOG_DIR="./logs"

# 缓存过期时间（秒）
# 缓存多久后检查是否需要更新
# 默认值：3600（1小时）
# 设为 0 则禁用缓存过期检查
CACHE_TTL=3600

# 最大并发下载数
# 默认值：5
MAX_CONCURRENT_DOWNLOADS=5

# HTTP 请求超时时间（秒）
# 默认值：30
HTTP_TIMEOUT=30

# 截图视口宽度
# 默认值：1920
SCREENSHOT_WIDTH=1920

# 截图视口高度
# 默认值：1080
SCREENSHOT_HEIGHT=1080

# ==============================================
# 开发配置
# ==============================================

# 调试模式
# 设为 "true" 启用调试模式
# 默认值：false
DEBUG="false"

# 启用热重载
# 设为 "true" 在开发时启用热重载
# 默认值：false
HOT_RELOAD="false"

# ==============================================
# 使用说明
# ==============================================
#
# 1. Cookie 安全：
#    - 妥善保管你的 LANHU_COOKIE
#    - 不要将 .env 文件提交到 git
#    - 定期更换 Cookie
#
# 2. 飞书集成：
#    - 可选但推荐用于团队协作
#    - 在飞书管理后台创建机器人
#    - 将机器人添加到群聊
#    - 复制 Webhook URL 到此处
#
# 3. 性能调优：
#    - 提高 MAX_CONCURRENT_DOWNLOADS 可加快批量操作速度
#    - 根据文档更新频率调整 CACHE_TTL
#    - 网络较慢时增加 HTTP_TIMEOUT
#
# 4. Docker 部署：
#    - 这些环境变量可通过 docker run -e 传递
#    - 或使用 docker-compose.yml 的 environment 配置
#
